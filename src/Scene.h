#pragma once
#include "GameState.h"

namespace Engine{

    /*
      Scene
      Abstraction for loading pre-created content
     */
    class Scene{
        GameState state;
        // TODO: create a state with file i/o etc.
    };
}
